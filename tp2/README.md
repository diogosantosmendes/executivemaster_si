**To run this project start the environment with the following commands**

start environment:
```sh
py -3.11 -m venv env-tp2
```
activate environment:
```sh
env-tp2\Scripts\activate
```
install packages:
```sh
pip install -r requirements.txt
```
